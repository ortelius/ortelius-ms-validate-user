apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-validate-user.name" . }}
spec:
  selector:
    app: {{ include "ms-validate-user.name" . }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
---
